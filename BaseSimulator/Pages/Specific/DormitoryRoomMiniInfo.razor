<table class="table">
    <tbody>
        <tr>
            <th scope="row">Morale Restored/hr</th>
            <td>@this.Game.ManpowerToString(this.Game.BaseData.DormData.Phases[this.Game.LevelToPhase(this.Level)].ManpowerRecover)</td>
        </tr>
        <tr>
            <th scope="row">Power Consumed</th>
            <td>@(-this.Room.Phases[this.Game.LevelToPhase(this.Level)].Electricity)</td>
        </tr>
    </tbody>
</table>

@code {
    [CascadingParameter] public Game Game { get; set; }
    [CascadingParameter] public Room Room { get; set; }
    [Parameter] public int Level { get; set; }
}
